apiVersion: v1
kind: Service

metadata:
  name: urlsecurenet-backend

spec:
  selector:
    app: urlsecurenet-backend

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000

  type: ClusterIP