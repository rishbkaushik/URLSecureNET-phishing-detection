apiVersion: v1
kind: Service

metadata:
  name: urlsecurenet-frontend

spec:
  selector:
    app: urlsecurenet-frontend

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000

  type: NodePort